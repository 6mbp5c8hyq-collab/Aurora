# AURORA Native iOS

Native SwiftUI iOS project for the AURORA industrial platform, connected to the Railway runtime.

Build locally with XcodeGen and Xcode. GitHub Actions runs the simulator build on pushes to `main`.
